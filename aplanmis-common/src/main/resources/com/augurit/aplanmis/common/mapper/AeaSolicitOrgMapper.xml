<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.augurit.aplanmis.common.mapper.AeaSolicitOrgMapper" >

    <!--1 返回所有字段的SQL字句 -->
    <sql id="allColumns" >
        SOLICIT_ORG_ID  solicitOrgId,
        ORG_ID  orgId,
        IS_BUS_SOLICIT isBusSolicit,
        STAGE_ID stageId,
        BUS_TYPE  busType,
        SOLICIT_TYPE  solicitType,
        CREATER  creater,
        CREATE_TIME  createTime,
        MODIFIER  modifier,
        MODIFY_TIME  modifyTime,
        ROOT_ORG_ID  rootOrgId,
        LATEST_STAGE_ID latestStageId
    </sql>

    <sql id="alias_allColumns">
        ${alias}.SOLICIT_ORG_ID  solicitOrgId,
        ${alias}.ORG_ID  orgId,
        ${alias}.IS_BUS_SOLICIT isBusSolicit,
        ${alias}.STAGE_ID stageId,
        ${alias}.BUS_TYPE  busType,
        ${alias}.SOLICIT_TYPE  solicitType,
        ${alias}.CREATER  creater,
        ${alias}.CREATE_TIME  createTime,
        ${alias}.MODIFIER  modifier,
        ${alias}.MODIFY_TIME  modifyTime,
        ${alias}.ROOT_ORG_ID  rootOrgId,
        ${alias}.LATEST_STAGE_ID latestStageId
    </sql>

    <!--2 根据主键ID查询单个实体 -->
    <select id="getAeaSolicitOrgById" resultType="AeaSolicitOrg" >
        select
           <include refid="allColumns"/>
        from  AEA_SOLICIT_ORG
        where SOLICIT_ORG_ID = #{id}
    </select>
    
    <select id="getSolicitOrgRelOrgInfoById" resultType="AeaSolicitOrg">
        select
            <include refid="alias_allColumns">
                <property name="alias" value="soliOrg"/>
            </include>,
            org.ORG_NAME orgName,
            org.ORG_CODE orgCode
        from AEA_SOLICIT_ORG soliOrg
        left join opu_om_org org on ( org.ORG_ID = soliOrg.ORG_ID and org.ORG_DELETED = '0' )
        where SOLICIT_ORG_ID = #{id}
    </select>

    <!--3 根据条件查询实体list -->
    <select id="listAeaSolicitOrg" resultType="AeaSolicitOrg">
        select
           <include refid="allColumns"/>
        from AEA_SOLICIT_ORG
        <where>
            <if test="solicitOrgId != null" >
            and    SOLICIT_ORG_ID = #{solicitOrgId}
            </if>
            <if test="orgId != null" >
            and    ORG_ID = #{orgId}
            </if>
            <if test="isBusSolicit != null" >
            and    IS_BUS_SOLICIT = #{isBusSolicit}
            </if>
            <if test="stageId != null" >
            and    STAGE_ID = #{stageId}
            </if>
            <if test="busType != null" >
            and    BUS_TYPE = #{busType}
            </if>
            <if test="solicitType != null" >
            and    SOLICIT_TYPE = #{solicitType}
            </if>
            <if test="creater != null" >
            and    CREATER = #{creater}
            </if>
            <if test="createTime != null" >
            and    CREATE_TIME = #{createTime}
            </if>
            <if test="modifier != null" >
            and    MODIFIER = #{modifier}
            </if>
            <if test="modifyTime != null" >
            and    MODIFY_TIME = #{modifyTime}
            </if>
            <if test="rootOrgId != null" >
            and    ROOT_ORG_ID = #{rootOrgId}
            </if>
            <if test="latestStageId != null" >
            and    LATEST_STAGE_ID  = #{latestStageId}
            </if>
        </where>
    </select>
    
    <select id="listAeaSolicitOrgRelOrgInfo" resultType="AeaSolicitOrg">
        select
            <include refid="alias_allColumns">
                <property name="alias" value="soliOrg"/>
            </include>,
            org.ORG_NAME orgName,
            org.ORG_CODE orgCode,
            org.ORG_PROPERTY orgProperty
        from AEA_SOLICIT_ORG soliOrg
        left join OPU_OM_ORG org on ( org.ORG_ID = soliOrg.ORG_ID and org.ORG_DELETED = '0' )
        <where>
            <if test="solicitOrgId != null" >
            and    soliOrg.SOLICIT_ORG_ID = #{solicitOrgId}
            </if>
            <if test="orgId != null" >
            and    soliOrg.ORG_ID = #{orgId}
            </if>
            <if test="isBusSolicit != null" >
            and    soliOrg.IS_BUS_SOLICIT = #{isBusSolicit}
            </if>
            <if test="stageId != null" >
            and    soliOrg.STAGE_ID = #{stageId}
            </if>
            <if test="busType != null" >
            and    soliOrg.BUS_TYPE = #{busType}
            </if>
            <if test="solicitType != null" >
            and    soliOrg.SOLICIT_TYPE = #{solicitType}
            </if>
            <if test="creater != null" >
            and    soliOrg.CREATER = #{creater}
            </if>
            <if test="createTime != null" >
            and    soliOrg.CREATE_TIME = #{createTime}
            </if>
            <if test="modifier != null" >
            and    soliOrg.MODIFIER = #{modifier}
            </if>
            <if test="modifyTime != null" >
            and    soliOrg.MODIFY_TIME = #{modifyTime}
            </if>
            <if test="rootOrgId != null" >
            and    soliOrg.ROOT_ORG_ID = #{rootOrgId}
            </if>
            <if test="latestStageId != null" >
            and    LATEST_STAGE_ID  = #{latestStageId}
            </if>
            <if test="keyword!=null and keyword!='' ">
            and (
                org.ORG_NAME like concat('%', concat( #{keyword}, '%')) or
                org.ORG_CODE like concat('%', concat( #{keyword}, '%'))
            )
            </if>
        </where>
        order by org.ORG_SORT_NO, soliOrg.CREATE_TIME desc
    </select>

    <!--5 新增实体对象 -->
    <insert id="insertAeaSolicitOrg" parameterType="AeaSolicitOrg" >
        insert into AEA_SOLICIT_ORG
        (
            SOLICIT_ORG_ID,
            ORG_ID,
            IS_BUS_SOLICIT,
            STAGE_ID,
            BUS_TYPE,
            SOLICIT_TYPE,
            CREATER,
            CREATE_TIME,
            MODIFIER,
            MODIFY_TIME,
            ROOT_ORG_ID,
            LATEST_STAGE_ID
        )
        values
        (
            #{solicitOrgId},
            #{orgId},
            #{isBusSolicit},
            #{stageId},
            #{busType},
            #{solicitType},
            #{creater},
            #{createTime},
            #{modifier},
            #{modifyTime},
            #{rootOrgId},
            #{latestStageId}
        )
    </insert>

    <!--6 修改实体对象 -->
    <update id="updateAeaSolicitOrg" parameterType="AeaSolicitOrg" >
        update AEA_SOLICIT_ORG
        <set >
            <if test="solicitOrgId != null" >
            SOLICIT_ORG_ID = #{solicitOrgId},
            </if>
            <if test="orgId != null" >
            ORG_ID = #{orgId},
            </if>
            <if test="isBusSolicit != null" >
            IS_BUS_SOLICIT = #{isBusSolicit},
            </if>
            <if test="stageId != null" >
            STAGE_ID = #{stageId},
            </if>
            <if test="busType != null" >
            BUS_TYPE = #{busType},
            </if>
            <if test="solicitType != null" >
            SOLICIT_TYPE = #{solicitType},
            </if>
            <if test="creater != null" >
            CREATER = #{creater},
            </if>
            <if test="createTime != null" >
            CREATE_TIME = #{createTime},
            </if>
            <if test="modifier != null" >
            MODIFIER = #{modifier},
            </if>
            <if test="modifyTime != null" >
            MODIFY_TIME = #{modifyTime},
            </if>
            <if test="rootOrgId != null" >
            ROOT_ORG_ID = #{rootOrgId},
            </if>
            <if test="latestStageId != null" >
            LATEST_STAGE_ID  = #{latestStageId},
            </if>
        </set>
        where SOLICIT_ORG_ID = #{solicitOrgId}
    </update>

    <delete id="deleteAeaSolicitOrg">
        delete from AEA_SOLICIT_ORG where SOLICIT_ORG_ID = #{id}
    </delete>
    
    <delete id="batchDelSolicitOrgByIds">
        <if test="ids!=null and ids!='' ">
            delete from AEA_SOLICIT_ORG where SOLICIT_ORG_ID in
            <foreach collection="ids" item="id" open="(" close=")" separator=",">
                #{id}
            </foreach>
        </if>
    </delete>

    <delete id="batchDelSolicitOrgByRootOrgId">
        delete from AEA_SOLICIT_ORG where ROOT_ORG_ID = #{rootOrgId}
    </delete>

    <update id="batchUpdateStageIdByThemeVerId">
        update AEA_SOLICIT_ORG
           set STAGE_ID = LATEST_STAGE_ID
         <where>
             and ROOT_ORG_ID = #{rootOrgId}
             and LATEST_STAGE_ID is not null
             and LATEST_STAGE_ID !=''
             and LATEST_STAGE_ID in (
                 select
                     STAGE_ID
                 from AEA_PAR_STAGE
                where IS_DELETED = '0'
                  and THEME_VER_ID = #{themeVerId}
                  and ROOT_ORG_ID = #{rootOrgId}
             )
         </where>
    </update>

    <resultMap type="AeaSolicitOrg" id="AeaSolicitOrgMap">
        <id property="solicitOrgId" column="SOLICIT_ORG_ID"/>
        <result property="orgId" column="ORG_ID"/>
        <result property="isBusSolicit" column="IS_BUS_SOLICIT"/>
        <result property="stageId" column="STAGE_ID"/>
        <result property="busType" column="BUS_TYPE"/>
        <result property="solicitType" column="SOLICIT_TYPE"/>
        <result property="creater" column="CREATER"/>
        <result property="createTime" column="CREATE_TIME"/>
        <result property="modifier" column="MODIFIER"/>
        <result property="modifyTime" column="MODIFY_TIME"/>
        <result property="rootOrgId" column="ROOT_ORG_ID"/>
        <result property="latestStageId" column="LATEST_STAGE_ID"/>
    </resultMap>

</mapper>